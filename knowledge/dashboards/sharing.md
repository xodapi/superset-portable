---
title: Публикация и экспорт
status: public
created: 2026-01-28
tags: [дашборды, экспорт, публикация, шаринг]
---

# Публикация и экспорт 📤

Способы делиться дашбордами с коллегами.

---

## Статусы публикации

| Статус | Видимость |
|--------|-----------|
| **Draft** | Только владельцы и админы |
| **Published** | Все пользователи с правами |

### Изменение статуса

1. Откройте дашборд
2. **⋮ → Manage dashboard**
3. Переключите **"Published"**

---

## Поделиться ссылкой

### Прямая ссылка

```
http://localhost:8088/superset/dashboard/123/
```

### Ссылка с фильтрами

Примените нужные фильтры и скопируйте URL — фильтры сохранятся.

### Постоянная ссылка

1. **⋮ → Copy dashboard URL**
2. URL включает версию настроек

---

## Экспорт изображения

### Весь дашборд

1. **⋮ → Download as Image**
2. Скачается PNG-файл

> [!NOTE]
> Для работы требуется установленный Selenium в некоторых версиях.

### Отдельная диаграмма

1. Наведите на диаграмму
2. **⋮ → Download as Image**

---

## Экспорт данных

### CSV

1. Наведите на диаграмму
2. **⋮ → Download → CSV**

### Excel (XLSX)

В некоторых версиях доступен экспорт в Excel.

### JSON

1. **⋮ → Download → JSON**
2. Данные в формате JSON

---

## Экспорт конфигурации

### Экспорт дашборда (YAML)

1. **⋮ → Export**
2. Скачается zip-архив с:
   - Конфигурация дашборда
   - Диаграммы
   - Датасеты
   - Подключения к БД (без паролей)

### Импорт

1. **Settings → Import Dashboards**
2. Выберите zip-файл
3. Настройте соответствие:
   - Базы данных
   - Владельцы

---

## Встраивание (Embed)

### Iframe

Встраивание дашборда на внешние страницы:

```html
<iframe
  src="http://localhost:8088/superset/dashboard/123/?standalone=true"
  width="100%"
  height="800"
  frameborder="0"
></iframe>
```

### Параметры URL

| Параметр | Описание |
|----------|----------|
| `?standalone=true` | Убрать навигацию |
| `?standalone=2` | Убрать всё кроме контента |
| `?show_filters=0` | Скрыть панель фильтров |

### Полный пример

```
/superset/dashboard/123/?standalone=true&show_filters=0
```

---

## Права доступа

### Кто может видеть

| Роль | Доступ |
|------|--------|
| **Admin** | Всё |
| **Alpha** | Свои + опубликованные |
| **Gamma** | Только опубликованные |
| **Public** | Публичные (если включено) |

### Настройка доступа

1. **⋮ → Manage dashboard**
2. Добавьте **Owners** — полный доступ
3. Используйте **Roles** для групп

Подробнее: [[Роли]]

---

## Расписание отчётов

### Report Scheduling

Автоматическая отправка по email:

1. **⋮ → Schedule Report**
2. Настройте:
   - Получатели
   - Расписание (cron)
   - Формат (PNG, PDF, CSV)

> [!NOTE]
> Требует настройки SMTP в `superset_config.py`

### Пример cron

| Выражение | Расписание |
|-----------|------------|
| `0 9 * * 1` | Пн 9:00 |
| `0 9 * * *` | Ежедневно 9:00 |
| `0 0 1 * *` | 1 число месяца |

---

## Alerting

### Настройка алертов

Уведомления при достижении условия:

1. **Settings → Alerts & Reports**
2. **"+ Alert"**
3. Настройте:
   - SQL-условие
   - Порог срабатывания
   - Канал уведомления

### Пример условия

```sql
SELECT COUNT(*) 
FROM orders 
WHERE status = 'failed' 
AND created_at > NOW() - INTERVAL '1 hour'
```

Алерт сработает, если COUNT > 0.

---

## PDF экспорт

### Требования

Для генерации PDF нужен headless браузер:

```python
# superset_config.py
WEBDRIVER_TYPE = "chrome"
WEBDRIVER_OPTION_ARGS = [
    "--headless",
    "--no-sandbox",
]
```

### Использование

1. **⋮ → Download as PDF**
2. Или в Report Schedule выберите формат PDF

---

## Советы

### ✅ Рекомендации

- Используйте постоянные ссылки для документации
- Экспортируйте конфигурацию для бэкапов
- Настройте алерты для критичных метрик
- Публикуйте только проверенные дашборды

### ❌ Избегайте

- Публикации незавершённых дашбордов
- Экспорта с чувствительными данными
- Встраивания без аутентификации

---

## См. также

- [[Работа с дашбордами]] — основы дашбордов
- [[Роли]] — настройка прав доступа
- [[Настройка конфигурации]] — SMTP и другие параметры
