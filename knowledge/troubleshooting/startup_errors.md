---
title: Ошибки запуска
status: public
created: 2026-01-28
tags: [troubleshooting, запуск, ошибки]
---

# Ошибки запуска 🚫

Проблемы при старте Superset и их решения.

---

## "Address already in use" / "Порт занят"

### Симптомы

```
OSError: [Errno 10048] Only one usage of each socket address is normally permitted
error while attempting to bind on address ('127.0.0.1', 8088)
```

### Причина

Порт 8088 уже используется другим процессом.

### Решение

1. Найдите процесс:
```cmd
netstat -ano | findstr :8088
```

Результат:
```
TCP    127.0.0.1:8088    0.0.0.0:0    LISTENING    12345
                                                    ^^^^^
                                                    PID
```

2. Завершите процесс:
```cmd
taskkill /PID 12345 /F
```

3. Или измените порт в конфигурации.

---

## "No module named superset"

### Симптомы

```
ModuleNotFoundError: No module named 'superset'
```

### Причина

Superset не установлен или PYTHONPATH неправильный.

### Решение

1. Проверьте установку:
```cmd
python\python.exe -c "import superset; print(superset.__version__)"
```

2. Если ошибка — переустановите:
```cmd
setup\install_superset.bat
```

---

## "ImportError: DLL load failed"

### Симптомы

```
ImportError: DLL load failed while importing _sqlite3
```

### Причина

Отсутствуют системные библиотеки.

### Решение

1. Установите [Visual C++ Redistributable](https://aka.ms/vs/17/release/vc_redist.x64.exe)
2. Перезагрузите компьютер
3. Попробуйте снова

---

## "Fatal error in launcher"

### Симптомы

```
Fatal error in launcher: Unable to create process using...
```

### Причина

Кириллица в пути к папке.

### Решение

Подробнее: [[Кириллица в путях]]

---

## "SECRET_KEY is not set"

### Симптомы

```
RuntimeError: A 'SECRET_KEY' must be set
```

### Причина

Не настроен секретный ключ.

### Решение

1. Убедитесь, что `superset_home/superset_config.py` существует
2. Проверьте наличие `SECRET_KEY`:
```python
SECRET_KEY = 'ваш-ключ-минимум-32-символа'
```

3. Сгенерируйте новый:
```python
import secrets
print(secrets.token_hex(32))
```

---

## "Database locked"

### Симптомы

```
sqlite3.OperationalError: database is locked
```

### Причина

Другой процесс использует superset.db.

### Решение

1. Закройте все процессы Superset:
```cmd
taskkill /IM python.exe /F
```

2. Подождите 5 секунд
3. Запустите снова

---

## "Connection refused" (health check)

### Симптомы

```
requests.exceptions.ConnectionError: Connection refused
```

### Причина

Superset ещё не запустился или упал.

### Решение

1. Подождите 30-60 секунд (первый запуск медленный)
2. Проверьте логи на ошибки
3. Увеличьте таймаут в лаунчере

---

## База данных повреждена

### Симптомы

```
sqlite3.DatabaseError: database disk image is malformed
```

### Причина

Файл superset.db повреждён.

### Решение

1. Восстановите из бэкапа (если есть)
2. Или удалите и создайте заново:
```cmd
del superset_home\superset.db
start_superset.bat
```

> [!WARNING]
> Все дашборды и настройки будут потеряны!

---

## Okno сразу закрывается

### Симптомы

Окно консоли появляется и сразу закрывается.

### Решение

1. Откройте CMD вручную
2. Перейдите в папку:
```cmd
cd C:\path\to\superset-portable
```

3. Запустите с паузой:
```cmd
start_superset.bat
pause
```

4. Прочитайте ошибку

---

## Нет доступа к localhost:8088

### Симптомы

Браузер показывает "Не удаётся получить доступ к сайту"

### Причины и решения

| Проверка | Решение |
|----------|---------|
| Superset запущен? | Проверьте консоль |
| Порт правильный? | Проверьте URL (8088) |
| Антивирус блокирует? | Добавьте исключение |
| VPN включён? | Отключите VPN |

### Проверка порта

```cmd
netstat -an | findstr :8088
```

Должно быть:
```
TCP    127.0.0.1:8088    0.0.0.0:0    LISTENING
```

---

## Python не найден

### Симптомы

```
'python' is not recognized as an internal or external command
```

### Причина

Папка `python/` отсутствует или повреждена.

### Решение

1. Скачайте Python Embeddable:
```powershell
.\setup\download_python.ps1
```

2. Переустановите Superset:
```cmd
.\setup\install_superset.bat
```

---

## Диагностика через лаунчер

### Подробные логи

```cmd
superset-launcher.exe --verbose
```

### Проверка конфигурации

```cmd
superset-launcher.exe validate
```

---

## См. также

- [[Решение проблем]] — общие проблемы
- [[Кириллица в путях]] — русские буквы
- [[Установка]] — переустановка
