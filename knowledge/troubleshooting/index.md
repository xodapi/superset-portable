---
title: Решение проблем
status: public
created: 2026-01-28
tags: [troubleshooting, проблемы, ошибки]
---

# Решение проблем 🔧

Общие проблемы и способы их решения.

---

## Быстрая диагностика

### Чек-лист

1. ✅ Порт 8088 свободен?
2. ✅ Python установлен?
3. ✅ Путь без кириллицы?
4. ✅ superset.db не повреждён?
5. ✅ Достаточно памяти?

### Просмотр логов

```cmd
type superset_output.log
```

Или в консоли при запуске через `start_superset.bat`.

---

## Проблемы запуска

### Superset не запускается

**Симптомы:** Окно сразу закрывается

**Решения:**

1. Запустите из консоли для просмотра ошибок:
```cmd
cd superset-portable
python\python.exe -c "from superset.app import create_app; app = create_app(); app.run(host='127.0.0.1', port=8088)"
```

2. Проверьте зависимости:
```cmd
python\python.exe -c "import superset; print('OK')"
```

Подробнее: [[Ошибки запуска]]

### Порт 8088 занят

**Симптомы:** `Address already in use`

**Решения:**

1. Найдите процесс:
```cmd
netstat -ano | findstr :8088
```

2. Завершите процесс:
```cmd
taskkill /PID <ID> /F
```

3. Или измените порт в `start_superset.bat`.

---

## Проблемы с данными

### Пустой датасет / No data

**Причины:**
- Фильтры отсеивают все данные
- Неправильный временной диапазон
- RLS ограничивает доступ

**Решения:**
1. Сбросьте все фильтры
2. Расширьте временной диапазон
3. Проверьте запрос в SQL Lab

### Таблица не видна

**Причины:**
- Нет прав на датасет
- Таблица в другой схеме
- Кеш устарел

**Решения:**
1. Проверьте права (Data → Datasets)
2. Укажите схему при подключении
3. **Data → Datasets → Refresh**

### Ошибка подключения к БД

**Симптомы:** `Connection failed`

**Решения:**
1. Проверьте хост и порт:
```cmd
ping hostname
telnet hostname port
```

2. Проверьте учётные данные
3. Проверьте firewall

---

## Проблемы с кириллицей

### Кракозябры вместо русского текста

**Причины:**
- Неправильная кодировка базы
- Неправильная кодировка CSV

**Решения:**

Для PostgreSQL:
```sql
SET client_encoding TO 'UTF8';
```

Для CSV-импорта:
- Укажите кодировку `UTF-8` или `windows-1251`

### Fatal error in launcher

Подробнее: [[Кириллица в путях]]

---

## Проблемы производительности

### Медленные запросы

**Причины:**
- Нет индексов
- Слишком много данных
- Сложный JOIN

**Решения:**
1. Добавьте `LIMIT`:
```sql
SELECT * FROM huge_table LIMIT 1000
```

2. Создайте индексы:
```sql
CREATE INDEX idx_date ON orders(order_date);
```

3. Используйте EXPLAIN:
```sql
EXPLAIN ANALYZE SELECT ...
```

### Браузер зависает

**Причины:**
- Слишком много строк
- Сложная визуализация

**Решения:**
1. Уменьшите количество строк
2. Упростите диаграмму
3. Используйте агрегацию

---

## Проблемы с диаграммами

### Диаграмма пустая

1. Проверьте фильтры
2. Проверьте временной диапазон
3. Выполните запрос в SQL Lab

### Ошибка рендеринга

1. Обновите страницу (`Ctrl+F5`)
2. Очистите кеш браузера
3. Проверьте консоль браузера (`F12`)

### Неправильные данные

1. Проверьте запрос в SQL Lab
2. Проверьте метрики и группировки
3. Проверьте фильтры

---

## Проблемы с дашбордами

### Дашборд не сохраняется

**Причины:**
- Нет прав на редактирование
- Ошибка валидации

**Решения:**
1. Проверьте права (вы Owner?)
2. Проверьте название (не пустое?)

### Фильтры не работают

1. Проверьте Scoping фильтра
2. Проверьте совместимость столбцов
3. Проверьте, что датасеты одинаковые

---

## Проблемы с SQL Lab

### Запрос не выполняется

1. Проверьте синтаксис SQL
2. Проверьте подключение к БД
3. Проверьте таймаут

### История запросов пустая

1. Запросы сохраняются автоматически
2. Проверьте фильтры в Query History

---

## Проблемы с памятью

### Out of memory

**Симптомы:** Процесс завершается

**Решения:**
1. Уменьшите лимит строк:
```python
# superset_config.py
ROW_LIMIT = 10000
```

2. Не используйте `SELECT *`
3. Добавьте агрегацию

---

## Сброс к заводским настройкам

### Полный сброс

> [!CAUTION]
> Все данные будут потеряны!

1. Удалите `superset_home/superset.db`
2. Запустите `start_superset.bat`
3. База будет создана заново

### Частичный сброс

Удалите только кеш:
```cmd
rd /s /q cache
```

---

## Получение помощи

### Логи для отчёта об ошибке

```cmd
type superset_output.log > bug_report.txt
type launch.log >> bug_report.txt
```

### Контакты

**Telegram:** [@serg_borisovich](https://t.me/serg_borisovich)

---

## См. также

- [[Ошибки запуска]] — проблемы при старте
- [[Кириллица в путях]] — русские буквы в пути
- [[FAQ]] — частые вопросы
