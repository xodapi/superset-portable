---
title: Кириллица в путях
status: public
created: 2026-01-28
tags: [troubleshooting, кириллица, unicode, пути]
---

# Кириллица в путях 🔤

Проблемы с русскими буквами в пути к папке.

---

## Проблема

Windows Python Embeddable плохо работает с путями, содержащими не-ASCII символы.

### Симптомы

```
Fatal error in launcher: Unable to create process using 
'C:\Пользователи\Иван\superset-portable\python\python.exe'
```

Или:

```
UnicodeDecodeError: 'ascii' codec can't decode byte
```

---

## Причина

Некоторые Python-библиотеки не поддерживают Unicode-пути на Windows.

---

## Решение

### Вариант 1: Переместить папку (рекомендуется)

Переместите `superset-portable` в путь без русских букв:

❌ **Плохо:**
```
C:\Пользователи\Иван\superset-portable\
C:\Users\Администратор\Desktop\супер-сет\
```

✅ **Хорошо:**
```
C:\superset\
C:\tools\superset-portable\
D:\analytics\superset\
```

### Вариант 2: Короткие имена Windows

Windows автоматически создаёт короткие имена (8.3) для папок:

```cmd
dir /x C:\
```

Результат:
```
ПОЛЬЗО~1    Пользователи
```

Можно использовать:
```
C:\ПОЛЬЗО~1\ИВАНОВ~1\superset-portable\
```

> [!NOTE]
> Короткие имена могут быть отключены на некоторых системах.

### Вариант 3: Symbolic Link

Создайте символическую ссылку:

```cmd
mklink /D C:\superset "C:\Пользователи\Иван\superset-portable"
```

Теперь используйте `C:\superset\`.

---

## Проверка текущего пути

Проверьте, есть ли кириллица:

```cmd
echo %CD%
```

Если видите русские буквы — нужно переместить папку.

---

## Кириллица в данных

### База данных

Данные с кириллицей работают нормально, если:
- База данных использует UTF-8
- Подключение настроено правильно

### PostgreSQL

```sql
SET client_encoding TO 'UTF8';
```

Или в строке подключения:
```
postgresql://user:pass@host:5432/db?client_encoding=utf8
```

### SQLite

Работает с UTF-8 по умолчанию.

### CSV-файлы

При импорте укажите кодировку:
- `UTF-8` — современные файлы
- `windows-1251` — старые файлы из Excel

---

## Кириллица в названиях

### Дашборды и диаграммы

✅ Работает:
```
Название: "Продажи по регионам"
```

### Датасеты

✅ Работает:
```
Название: "Заказы клиентов"
```

### Столбцы

✅ Работает в большинстве случаев:
```sql
SELECT имя, фамилия FROM пользователи
```

> [!WARNING]
> Некоторые базы данных плохо работают с кириллицей в названиях столбцов. Рекомендуется латиница.

---

## Профили Windows

### Проблема с профилем

Если имя пользователя Windows кириллическое:
```
C:\Users\Иван\
```

Superset попытается записать файлы в этот путь.

### Решение

1. Установите Superset в другое место:
```
C:\superset\
```

2. Укажите путь к home:
```python
# superset_config.py
import os
os.environ['SUPERSET_HOME'] = 'C:\\superset\\superset_home'
```

---

## Переменные окружения

### PYTHONPATH

Если указан кириллический путь в PYTHONPATH — могут быть проблемы.

### Проверка

```cmd
echo %PYTHONPATH%
```

### Очистка

```cmd
set PYTHONPATH=
```

---

## Итого: чек-лист

| Проверка | Что делать |
|----------|----------|
| Путь к superset-portable | Без кириллицы |
| Имя пользователя Windows | Явно указать SUPERSET_HOME |
| PYTHONPATH | Очистить или проверить |
| CSV-файлы | Указать кодировку UTF-8 |
| Названия столбцов в БД | Латиница рекомендуется |

---

## См. также

- [[Ошибки запуска]] — другие проблемы
- [[Решение проблем]] — общие проблемы
- [[Загрузка Excel]] — импорт данных
