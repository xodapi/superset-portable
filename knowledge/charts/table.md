---
title: Таблица
status: public
created: 2026-01-28
tags: [диаграммы, таблица, данные]
---

# Таблица 📋

Табличное представление — базовая и самая универсальная визуализация.

---

## Когда использовать

✅ **Подходит для:**
- Детальный просмотр данных
- Экспорт в Excel/CSV
- Поиск конкретных записей
- Сортировка по столбцам

❌ **Не подходит для:**
- Показ трендов
- Сравнение пропорций
- Визуализация больших объёмов

---

## Создание таблицы

### Шаг 1: Выбор типа

1. **"+" → Chart**
2. Выберите датасет
3. Тип: **"Table"**

### Шаг 2: Выбор столбцов

В секции **Query → Columns** добавьте нужные столбцы:

```
name, email, created_at, status
```

### Шаг 3: Метрики (опционально)

Добавьте агрегатные метрики:
- `COUNT(*)` — количество
- `SUM(amount)` — сумма

### Шаг 4: Группировка

Если есть метрики, добавьте группировку в **Group by**.

---

## Настройки таблицы

### Сортировка

| Параметр | Описание |
|----------|----------|
| **Order by** | Столбец для сортировки |
| **Order descending** | По убыванию |

### Лимит строк

| Параметр | Описание |
|----------|----------|
| **Row limit** | Максимум строк (по умолчанию 10,000) |
| **Server pagination** | Постраничная загрузка |

---

## Форматирование

### Условное форматирование

В **Customize → Cell Bars** можно добавить полоски для числовых столбцов.

### Выравнивание

| Тип данных | Рекомендуемое выравнивание |
|------------|---------------------------|
| Текст | По левому краю |
| Числа | По правому краю |
| Даты | По центру |

### Формат чисел

В настройках столбца укажите формат:

| Формат | Результат |
|--------|-----------|
| `.2f` | 1234.56 |
| `,.0f` | 1,235 |
| `.2%` | 45.67% |
| `$,.2f` | $1,234.56 |

---

## Фильтрация и поиск

### Встроенный поиск

В режиме просмотра таблица поддерживает:
- Поиск по тексту (верхняя панель)
- Сортировка по клику на заголовок
- Постраничная навигация

### Фильтры в запросе

В секции **Filters** добавьте условия:

```sql
status = 'active' AND amount > 1000
```

---

## Группировка с подытогами

### Pivot Table

Для сводных таблиц используйте тип **"Pivot Table"**:

1. **Rows** — строки сводной таблицы
2. **Columns** — столбцы сводной
3. **Metrics** — значения в ячейках

### Пример

| | Январь | Февраль | Март |
|---|--------|---------|------|
| **Москва** | 1,500 | 1,800 | 2,100 |
| **СПб** | 900 | 1,100 | 1,300 |
| **Всего** | 2,400 | 2,900 | 3,400 |

---

## Экспорт данных

### Из диаграммы

1. Откройте диаграмму
2. Меню **"⋮" → Download**
3. Выберите формат:
   - **CSV** — для Excel
   - **JSON** — для программной обработки

### Из дашборда

1. Наведите на диаграмму
2. Нажмите **"⋮" → Download as CSV**

---

## Ссылки в ячейках

Можно добавить кликабельные ссылки:

### В датасете

```sql
SELECT 
    name,
    '<a href="/details/' || id || '">' || name || '</a>' as link
FROM users
```

### В настройках столбца

Включите **"Render as HTML"** в настройках столбца.

> [!WARNING]
> Будьте осторожны с HTML — это может быть уязвимостью XSS.

---

## Производительность

### Для больших таблиц

1. Используйте **Server Pagination**
2. Ограничивайте количество строк `LIMIT 1000`
3. Создайте индексы в базе данных

### Кеширование

Включите кеширование в настройках диаграммы:
- **Cache Timeout** — время жизни кеша в секундах

---

## Решение проблем

### Таблица пустая

1. Проверьте фильтры — могут отсеивать все данные
2. Проверьте временной диапазон
3. Выполните запрос в SQL Lab

### Столбцы не отображаются

1. Добавьте столбцы в **Query → Columns**
2. Или уберите **Group by** если не нужна агрегация

### Медленная загрузка

1. Добавьте `LIMIT`
2. Включите постраничную загрузку
3. Создайте индексы в БД

---

## См. также

- [[Создание диаграмм]] — обзор визуализаций
- [[Гистограмма]] — для сравнения категорий
- [[SQL Lab]] — создание датасетов
