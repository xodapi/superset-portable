---
title: Big Number (KPI)
status: public
created: 2026-01-28
tags: [диаграммы, kpi, big-number, показатели]
---

# Big Number (KPI) 🎯

Крупные числа для ключевых показателей эффективности.

---

## Когда использовать

✅ **Подходит для:**
- Ключевые метрики (выручка, конверсия, NPS)
- Сравнение с предыдущим периодом
- Заголовки дашбордов
- Мгновенное понимание статуса

❌ **Не подходит для:**
- Детальный анализ
- Много показателей (используйте таблицу)

---

## Типы

### Big Number

Одно большое число:

```
┌─────────────────────┐
│                     │
│    ₽ 12,450,000     │
│                     │
│    Выручка Q1       │
└─────────────────────┘
```

### Big Number with Trendline

С мини-графиком тренда:

```
┌─────────────────────┐
│   ₽ 12,450,000      │
│   ▲ +15.3%          │
│   ╱─────╲╱──╲╱──    │
│   Выручка Q1        │
└─────────────────────┘
```

---

## Создание

### Шаг 1: Выбор типа

1. **"+" → Chart**
2. Выберите датасет
3. Тип: **"Big Number"** или **"Big Number with Trendline"**

### Шаг 2: Настройка

| Параметр | Описание | Пример |
|----------|----------|--------|
| **Metric** | Основной показатель | `SUM(revenue)` |
| **Time Grain** | Гранулярность (для тренда) | `Month` |
| **Time Range** | Период | `Last quarter` |

---

## Сравнение с прошлым периодом

### Настройка

В **Advanced → Time Comparison**:

| Параметр | Значение |
|----------|----------|
| **Time Shift** | `1 year ago`, `1 month ago` |
| **Comparison Type** | `Absolute`, `Percentage` |

### Отображение

```
   ₽ 12,450,000
   ▲ +15.3% vs прошлый год
```

Или:
```
   ₽ 12,450,000
   +1,650,000 vs прошлый месяц
```

---

## Форматирование

### Формат числа

В **Customize → Number Format**:

| Формат | Результат |
|--------|-----------|
| `,.0f` | 12,450,000 |
| `.2s` | 12.45M |
| `.3s` | 12.5M |
| `$,.0f` | $12,450,000 |
| `.1%` | 45.3% |

### Для российского формата

Используйте `,.0f` и замените разделитель или:
```
CONCAT(FORMAT(value/1000000, 2), ' млн ₽')
```

---

## Цвет по условию

### Conditional Formatting

Меняйте цвет в зависимости от значения:

| Условие | Цвет |
|---------|------|
| > цели | 🟢 Зелёный |
| = цели (±5%) | 🟡 Жёлтый |
| < цели | 🔴 Красный |

### Настройка

В **Customize → Conditional Formatting**:

```json
[
    {"op": ">=", "value": 10000000, "color": "#2ecc71"},
    {"op": ">=", "value": 8000000, "color": "#f1c40f"},
    {"op": "<", "value": 8000000, "color": "#e74c3c"}
]
```

---

## Мини-график (Sparkline)

### Настройка тренда

| Параметр | Описание |
|----------|----------|
| **Time Grain** | Гранулярность точек |
| **Trendline** | Показать/скрыть |
| **Comparison** | Сравнение периодов |

### Стиль линии

- **Line** — линейный
- **Area** — с заливкой

---

## Подзаголовок

### Добавление описания

В **Customize → Subheader**:

```
Выручка за Q1 2026
```

### Динамический подзаголовок

Через SQL в датасете:
```sql
SELECT 
    SUM(revenue) as revenue,
    'Данные на ' || date(MAX(created_at)) as period
FROM sales
```

---

## Группа KPI

Для нескольких показателей на одном дашборде:

```
┌────────────┬────────────┬────────────┐
│ ₽ 12.4M    │   1,250    │   4.5%     │
│ Выручка    │   Заказы   │   Конверсия│
│ ▲ +15%     │   ▲ +8%    │   ▼ -0.3%  │
└────────────┴────────────┴────────────┘
```

**Как сделать:**
1. Создайте 3 отдельных Big Number
2. Разместите в ряд на дашборде
3. Используйте одинаковую высоту

---

## Примеры

### Выручка

```sql
SELECT SUM(amount) as revenue
FROM sales
WHERE date >= '2026-01-01'
```

Формат: `$,.0f`

### Конверсия

```sql
SELECT 
    COUNT(CASE WHEN status = 'completed' THEN 1 END) * 100.0 / 
    NULLIF(COUNT(*), 0) as conversion_rate
FROM orders
```

Формат: `.1%`

### Средний чек

```sql
SELECT AVG(amount) as avg_order
FROM sales
WHERE status = 'completed'
```

Формат: `₽ ,.0f`

---

## Советы

### ✅ Рекомендации

- Один показатель = один Big Number
- Добавляйте контекст (% изменения, сравнение)
- Используйте цветовую индикацию
- Размещайте вверху дашборда

### ❌ Избегайте

- Слишком много Big Number рядом (>5)
- Показателей без контекста
- Мелкого размера (теряется эффект)

---

## См. также

- [[Создание диаграмм]] — обзор визуализаций
- [[Работа с дашбордами]] — компоновка
- [[Линейный график]] — детальный тренд
