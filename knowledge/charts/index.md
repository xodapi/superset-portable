---
title: Создание диаграмм
status: public
created: 2026-01-28
tags: [диаграммы, визуализации, графики]
---

# Создание диаграмм 📈

Apache Superset поддерживает более 40 типов визуализаций.

---

## Типы визуализаций

### Таблицы и списки
- [[Таблица]] — табличное представление данных
- **Pivot Table** — сводная таблица
- **Word Cloud** — облако слов

### Графики
- [[Гистограмма]] — столбчатые диаграммы
- [[Линейный график]] — временные ряды
- **Area Chart** — диаграмма с областями
- **Scatter Plot** — точечная диаграмма

### Доли и проценты
- [[Круговая диаграмма]] — pie/donut
- **Treemap** — древовидная карта
- **Sunburst** — солнечная диаграмма

### Показатели
- [[Big Number]] — крупные KPI
- **Gauge** — спидометр
- **Bullet Chart** — диаграмма с целью

### Карты
- [[Карта]] — геопространственные данные
- **Deck.gl Scatter** — точки на карте
- **Deck.gl Path** — маршруты

---

## Создание диаграммы

### Шаг 1: Выбор датасета

1. Нажмите **"+" → Chart**
2. Выберите датасет из списка
3. Выберите тип визуализации

### Шаг 2: Настройка запроса

В левой панели настройте:

| Секция | Описание |
|--------|----------|
| **Time** | Временной диапазон и гранулярность |
| **Query** | Метрики и группировки |
| **Filters** | Условия фильтрации |

### Шаг 3: Выполнение

Нажмите **"Run Query"** (▶) или `Ctrl+Enter`.

### Шаг 4: Настройка внешнего вида

В разделе **"Customize"**:
- Цвета и палитры
- Подписи и легенда
- Размеры и отступы

### Шаг 5: Сохранение

1. **Save**
2. Введите название
3. Выберите дашборд (или создайте новый)

---

## Метрики (Metrics)

Метрики — это агрегатные вычисления над данными.

### Простые метрики

| Метрика | SQL эквивалент |
|---------|----------------|
| COUNT(*) | `SELECT COUNT(*)` |
| SUM(column) | `SELECT SUM(column)` |
| AVG(column) | `SELECT AVG(column)` |
| MIN(column) | `SELECT MIN(column)` |
| MAX(column) | `SELECT MAX(column)` |

### Кастомные метрики

Нажмите **"Custom SQL"** и введите выражение:

```sql
SUM(amount) / NULLIF(COUNT(*), 0)
```

### Процентные метрики

```sql
SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
```

---

## Группировка (Dimensions)

Группировки разбивают данные по категориям.

### Примеры

- **По дате**: `date`, `month`, `year`
- **По категории**: `product`, `region`, `status`
- **По иерархии**: `country → city → district`

### Группировка по времени

Для временных данных используйте **Time Grain**:

| Гранулярность | Пример |
|---------------|--------|
| Day | 2026-01-28 |
| Week | 2026-W04 |
| Month | 2026-01 |
| Quarter | 2026-Q1 |
| Year | 2026 |

---

## Фильтры в диаграммах

### Простой фильтр

1. В секции **Filters** нажмите **"+"**
2. Выберите столбец
3. Укажите условие (`=`, `!=`, `>`, `<`, `IN`)
4. Введите значение

### Временной фильтр

Используйте **Time Range**:
- `Last 7 days` — последние 7 дней
- `Last month` — прошлый месяц
- `Previous Year` — прошлый год
- `Custom` — произвольный диапазон

### SQL фильтр

Для сложных условий:

```sql
amount > 1000 AND status IN ('active', 'pending')
```

---

## Цветовые палитры

Superset включает несколько палитр:

| Палитра | Для чего |
|---------|----------|
| **Categorical** | Дискретные категории |
| **Sequential** | Числовые градиенты |
| **Diverging** | От минимума к максимуму |
| **Google** | 8 ярких цветов |
| **Superset** | Синяя палитра по умолчанию |

### Кастомные цвета

В **Customize → Color Scheme** можно указать:

```json
{"Продукт A": "#FF5733", "Продукт B": "#33FF57"}
```

---

## Подписи данных

### Включение подписей

В **Customize**:
- **Show Labels** — показать значения
- **Label Type** — формат (значение, процент, имя)
- **Number Format** — формат чисел

### Форматирование чисел

| Формат | Пример |
|--------|--------|
| `.2f` | 1234.56 |
| `,.0f` | 1,235 |
| `.2%` | 12.35% |
| `$,.2f` | $1,234.56 |
| `.2s` | 1.2k |

---

## Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Ctrl + Enter` | Выполнить запрос |
| `Ctrl + S` | Сохранить |
| `Ctrl + Z` | Отменить |

---

## Советы

### ✅ Рекомендации

- Начинайте с простых визуализаций (таблица, гистограмма)
- Добавляйте фильтры для больших датасетов
- Используйте понятные названия метрик
- Тестируйте на разных временных диапазонах

### ❌ Избегайте

- Слишком много элементов на одном графике (>10 серий)
- Круговых диаграмм с >6 сегментами
- 3D-визуализаций (сложно читать)

---

## См. также

- [[Таблица]] — табличное представление
- [[Гистограмма]] — столбчатые диаграммы  
- [[Линейный график]] — временные ряды
- [[Работа с дашбордами]] — добавление на дашборд
- [[Быстрый старт]] — первые шаги
