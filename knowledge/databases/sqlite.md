---
title: SQLite
status: public
created: 2026-01-28
tags: [базы-данных, sqlite, встроенная]
---

# SQLite 🗃️

SQLite — встроенная файловая база данных, идеально подходящая для локальной работы.

---

## Преимущества SQLite

- ✅ Не требует установки сервера
- ✅ Один файл = одна база данных
- ✅ Работает оффлайн
- ✅ Быстрая для небольших объёмов данных
- ✅ Поддерживает полнотекстовый поиск (FTS5)

---

## Встроенные базы данных

Портативная версия включает:

| База | Путь | Описание |
|------|------|----------|
| **examples.db** | `./examples.db` | Демо-данные |
| **superset.db** | `./superset_home/superset.db` | Метаданные Superset |

---

## Подключение SQLite

### Строка подключения

```
sqlite:///путь/к/файлу.db
```

### Примеры

**Абсолютный путь (Windows):**
```
sqlite:///C:/data/mydata.db
```

**Относительный путь:**
```
sqlite:///./data/mydata.db
```

**В одной папке с Superset:**
```
sqlite:///./mydata.db
```

> [!TIP]
> Для Windows используйте прямые слеши `/` или двойные обратные `\\`.

---

## Создание новой базы SQLite

### Способ 1: Через Python

```python
import sqlite3

conn = sqlite3.connect('mydata.db')
cursor = conn.cursor()

# Создание таблицы
cursor.execute('''
    CREATE TABLE sales (
        id INTEGER PRIMARY KEY,
        date TEXT,
        product TEXT,
        amount REAL,
        quantity INTEGER
    )
''')

# Добавление данных
cursor.execute('''
    INSERT INTO sales (date, product, amount, quantity)
    VALUES ('2026-01-28', 'Товар A', 1500.50, 10)
''')

conn.commit()
conn.close()
```

### Способ 2: Через DBeaver / DB Browser

1. Скачайте [DB Browser for SQLite](https://sqlitebrowser.org/)
2. Создайте новую базу данных
3. Добавьте таблицы и данные

---

## Импорт CSV в SQLite

### Через командную строку sqlite3

```cmd
sqlite3 mydata.db
.mode csv
.import data.csv tablename
.quit
```

### Через Python

```python
import sqlite3
import csv

conn = sqlite3.connect('mydata.db')
cursor = conn.cursor()

with open('data.csv', 'r', encoding='utf-8') as f:
    reader = csv.DictReader(f)
    for row in reader:
        cursor.execute(
            'INSERT INTO mytable VALUES (?, ?, ?)',
            (row['col1'], row['col2'], row['col3'])
        )

conn.commit()
conn.close()
```

---

## Типы данных SQLite

| Тип SQLite | Описание | Пример |
|------------|----------|--------|
| **INTEGER** | Целые числа | `42`, `-100` |
| **REAL** | Числа с плавающей точкой | `3.14`, `1500.50` |
| **TEXT** | Строки | `'Привет'` |
| **BLOB** | Бинарные данные | Изображения, файлы |
| **NULL** | Отсутствие значения | `NULL` |

---

## Работа с датами

SQLite не имеет встроенного типа DATE. Используйте TEXT в формате ISO:

```sql
-- Хранение
INSERT INTO events (event_date) VALUES ('2026-01-28')

-- Фильтрация
SELECT * FROM events 
WHERE event_date >= '2026-01-01' 
  AND event_date < '2026-02-01'

-- Извлечение года
SELECT strftime('%Y', event_date) as year FROM events
```

---

## Ограничения SQLite

| Параметр | Ограничение |
|----------|-------------|
| Размер базы | до 281 TB |
| Размер строки | до 1 GB |
| Столбцов в таблице | до 2000 |
| Параллельные записи | ❌ Только одна |

> [!WARNING]
> SQLite блокирует файл при записи. Для многопользовательского доступа используйте PostgreSQL или MySQL.

---

## Оптимизация производительности

### Создание индексов

```sql
CREATE INDEX idx_sales_date ON sales(date);
CREATE INDEX idx_sales_product ON sales(product);
```

### VACUUM (сжатие базы)

```sql
VACUUM;
```

### ANALYZE (обновление статистики)

```sql
ANALYZE;
```

---

## Решение проблем

### "database is locked"

Другой процесс использует базу. Закройте все приложения, работающие с этим файлом.

### "no such table"

Таблица не существует. Проверьте:
```sql
.tables
```

### Кириллица отображается некорректно

Убедитесь, что база создана с кодировкой UTF-8:
```sql
PRAGMA encoding = 'UTF-8';
```

---

## См. также

- [[Подключение базы данных]] — обзор подключений
- [[Загрузка Excel]] — импорт CSV файлов
- [[Быстрый старт]] — первые шаги
