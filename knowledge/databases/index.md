---
title: Подключение базы данных
status: public
created: 2026-01-28
tags: [базы-данных, подключение, sqlalchemy]
---

# Подключение базы данных 🔌

Apache Superset поддерживает десятки различных баз данных через SQLAlchemy.

---

## Поддерживаемые базы данных

| База данных | Драйвер | Статус |
|-------------|---------|--------|
| **SQLite** | Встроен | ✅ Готово |
| **PostgreSQL** | psycopg2 | ✅ Готово |
| **MySQL** | mysqlclient | ✅ Готово |
| **ClickHouse** | clickhouse-driver | ✅ Готово |
| **DuckDB** | duckdb-engine | ✅ Готово |
| **MS SQL Server** | pymssql | ⚠️ Требует установки |
| **Oracle** | cx_oracle | ⚠️ Требует установки |

---

## Как подключить базу данных

### Шаг 1: Открыть диалог

1. Нажмите **"+"** в правом верхнем углу
2. Выберите **"Data → Connect Database"**

### Шаг 2: Выбрать тип

Выберите вашу базу данных из списка или используйте **"Other"** для ручного ввода.

### Шаг 3: Ввести строку подключения

Формат **SQLAlchemy URI**:

```
dialect+driver://username:password@host:port/database
```

### Шаг 4: Тестирование

Нажмите **"Test Connection"** для проверки.

---

## Примеры строк подключения

### SQLite (локальный файл)

```
sqlite:///C:/data/mydata.db
```

Для относительного пути:
```
sqlite:///./data/mydata.db
```

Подробнее: [[SQLite]]

### PostgreSQL

```
postgresql://user:password@localhost:5432/mydatabase
```

С SSL:
```
postgresql://user:password@host:5432/db?sslmode=require
```

Подробнее: [[PostgreSQL]]

### MySQL

```
mysql://user:password@localhost:3306/mydatabase?charset=utf8mb4
```

### ClickHouse

```
clickhouse+native://user:password@localhost:9000/mydatabase
```

Подробнее: [[ClickHouse]]

### DuckDB

```
duckdb:///C:/data/analytics.duckdb
```

---

## Настройки подключения

### Дополнительные параметры

В разделе **"Advanced"** можно настроить:

| Параметр | Описание |
|----------|----------|
| **Schema** | Схема по умолчанию |
| **Cache Timeout** | Время кеширования метаданных |
| **Allow CSV Upload** | Разрешить загрузку CSV в эту БД |
| **Async** | Асинхронное выполнение запросов |

### Безопасность

> [!WARNING]
> Не используйте пользователя с правами DELETE/DROP для аналитики!

Рекомендации:
- Создайте отдельного пользователя только для чтения
- Ограничьте доступ к конкретным схемам/таблицам
- Используйте SSL для удалённых подключений

---

## Проблемы подключения

### "Connection failed"

1. Проверьте доступность хоста: `ping hostname`
2. Проверьте порт: `telnet hostname port`
3. Проверьте логин/пароль
4. Проверьте firewall

### "No module named..."

Драйвер не установлен. Для установки:

```cmd
python\python.exe -m pip install driver-name
```

### Медленные запросы

1. Добавьте индексы в базу данных
2. Используйте `LIMIT` в запросах
3. Настройте кеширование в Superset

---

## См. также

- [[SQLite]] — встроенная база данных
- [[PostgreSQL]] — настройка подключения
- [[ClickHouse]] — аналитическая СУБД
- [[Загрузка Excel]] — импорт файлов
- [[Быстрый старт]] — первые шаги
