---
title: Загрузка Excel и CSV
status: public
created: 2026-01-28
tags: [данные, excel, csv, импорт]
---

# Загрузка Excel и CSV 📊

Superset позволяет загружать данные из файлов напрямую в браузере.

---

## Поддерживаемые форматы

| Формат | Расширения | Примечание |
|--------|------------|------------|
| **CSV** | `.csv` | Рекомендуется |
| **TSV** | `.tsv` | Разделитель табуляция |
| **Excel** | `.xlsx`, `.xls` | Требует openpyxl |
| **Columnar** | `.parquet` | Быстрый для больших файлов |

---

## Загрузка CSV

### Шаг 1: Открыть интерфейс загрузки

1. **Data → Upload a CSV**
2. Или **"+" → Data → Upload a CSV**

### Шаг 2: Выбрать файл

Нажмите **"Select"** и выберите CSV-файл.

### Шаг 3: Настройки

| Параметр | Описание | Пример |
|----------|----------|--------|
| **Table Name** | Название таблицы в БД | `sales_2026` |
| **Database** | Куда загружать (обычно examples) | `examples` |
| **Schema** | Схема (можно пустое) | `public` |
| **Delimiter** | Разделитель | `,` или `;` |
| **Header Row** | Первая строка — заголовки | ✅ |
| **Decimal Character** | Десятичный разделитель | `.` или `,` |

### Шаг 4: Загрузка

Нажмите **"Upload"**. После завершения таблица появится в списке датасетов.

---

## Параметры импорта

### Разделители

| Регион | Разделитель столбцов | Десятичный |
|--------|---------------------|------------|
| **США/UK** | `,` (запятая) | `.` (точка) |
| **Европа/Россия** | `;` (точка с запятой) | `,` (запятая) |

### Кодировка

По умолчанию UTF-8. Для файлов из Excel (Windows) может потребоваться:
- `windows-1251` — для русского
- `cp1252` — для западноевропейских языков

---

## Подготовка CSV-файла

### Рекомендации

✅ **Делайте**:
- Первая строка — названия столбцов (латиница без пробелов)
- Даты в формате `YYYY-MM-DD` (2026-01-28)
- Числа без пробелов и валют
- Кодировка UTF-8

❌ **Избегайте**:
- Объединённых ячеек
- Пустых строк в середине
- Спецсимволов в названиях столбцов

### Пример хорошего CSV

```csv
date,product,quantity,amount,region
2026-01-01,Товар A,10,15000.50,Москва
2026-01-01,Товар B,5,7500.25,Санкт-Петербург
2026-01-02,Товар A,8,12000.40,Москва
```

### Преобразование из Excel

1. Откройте файл в Excel
2. **Файл → Сохранить как**
3. Тип файла: **CSV UTF-8 (с разделителями-запятыми)**

---

## Загрузка Excel напрямую

> [!NOTE]
> Требуется установка библиотеки `openpyxl`.

### Установка

```cmd
python\python.exe -m pip install openpyxl
```

### Использование

1. **Data → Upload Excel File**
2. Выберите `.xlsx` файл
3. Укажите номер листа (Sheet)
4. Нажмите **"Upload"**

---

## Загрузка больших файлов

Для файлов >10 MB рекомендуется:

### Вариант 1: Через SQLite

```python
import pandas as pd
import sqlite3

# Читаем CSV
df = pd.read_csv('huge_file.csv')

# Записываем в SQLite
conn = sqlite3.connect('mydata.db')
df.to_sql('tablename', conn, if_exists='replace', index=False)
conn.close()
```

Затем подключите `mydata.db` как базу данных.

### Вариант 2: Через DuckDB

```python
import duckdb

# DuckDB автоматически распознаёт схему
duckdb.sql("""
    CREATE TABLE sales AS 
    SELECT * FROM 'huge_file.csv'
""")
```

---

## Автоматическое определение типов

Superset пытается определить типы столбцов автоматически:

| Данные | Определяемый тип |
|--------|-----------------|
| `123`, `456` | INTEGER |
| `123.45`, `67.89` | FLOAT |
| `2026-01-28` | DATE |
| `текст` | VARCHAR |

### Принудительное указание типов

В расширенных настройках CSV-импорта можно указать:

```json
{
  "Datatype": {
    "order_id": "INTEGER",
    "amount": "FLOAT",
    "created_at": "DATETIME"
  }
}
```

---

## Обновление данных

### Перезаписать таблицу

1. Загрузите файл с тем же именем таблицы
2. Отметьте **"Table Exists: Replace"**

### Добавить данные

1. Загрузите файл
2. Отметьте **"Table Exists: Append"**

> [!WARNING]
> При Append структура столбцов должна совпадать!

---

## Решение проблем

### "Could not determine delimiter"

Укажите разделитель вручную в настройках.

### Кракозябры вместо русского текста

Укажите кодировку:
- `utf-8` — современные файлы
- `windows-1251` — старые файлы из Excel

### "Permission denied"

1. Проверьте, что CSV-загрузка разрешена для базы данных
2. В настройках БД включите **"Allow Data Upload"**

### Числа определились как текст

Проверьте:
- Нет пробелов в числах
- Правильный десятичный разделитель
- Нет символов валют (₽, $)

---

## См. также

- [[Подключение базы данных]] — другие источники данных
- [[SQLite]] — локальная база данных
- [[Быстрый старт]] — первые шаги
