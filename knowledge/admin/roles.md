---
title: Роли и права доступа
status: public
created: 2026-01-28
tags: [администрирование, роли, безопасность, rbac]
---

# Роли и права доступа 🔐

Role-Based Access Control (RBAC) в Superset.

---

## Встроенные роли

| Роль | Описание |
|------|----------|
| **Admin** | Полный доступ ко всему |
| **Alpha** | Создание и редактирование без админ-функций |
| **Gamma** | Только просмотр опубликованного |
| **sql_lab** | Доступ к SQL Lab |
| **Public** | Публичный доступ (если включён) |

---

## Подробно о ролях

### Admin

Полные права:
- ✅ Управление пользователями
- ✅ Управление подключениями БД
- ✅ Доступ ко всем дашбордам
- ✅ Настройки системы

### Alpha

Продвинутый пользователь:
- ✅ Создание датасетов
- ✅ Создание диаграмм
- ✅ Создание дашбордов
- ✅ SQL Lab
- ❌ Управление пользователями
- ❌ Настройки системы

### Gamma

Базовый просмотр:
- ✅ Просмотр опубликованных дашбордов
- ✅ Просмотр разрешённых датасетов
- ❌ Создание контента
- ❌ SQL Lab (по умолчанию)

### sql_lab

Дополнительная роль:
- ✅ Доступ к SQL Lab
- Комбинируется с Gamma для аналитиков

---

## Создание роли

### Через интерфейс

1. **Settings → List Roles**
2. **"+ Role"**
3. Введите название
4. Выберите разрешения (Permissions)
5. **Save**

### Копирование существующей

1. Откройте существующую роль
2. Скопируйте разрешения
3. Создайте новую роль с этими разрешениями

---

## Разрешения (Permissions)

### Структура

```
[действие] on [объект]
```

### Примеры

| Разрешение | Описание |
|------------|----------|
| `can read on Dashboard` | Просмотр дашбордов |
| `can write on Dashboard` | Редактирование дашбордов |
| `can read on Chart` | Просмотр диаграмм |
| `datasource access on [db_name].[table]` | Доступ к датасету |

### Категории

| Категория | Примеры |
|-----------|---------|
| **Database** | `database access on [examples]` |
| **Datasource** | `datasource access on [examples].[birth_names]` |
| **Dashboard** | `can read on Dashboard` |
| **Chart** | `can read on Slice` |
| **SQL Lab** | `can read on SqlLab` |

---

## Доступ к данным

### Уровни

```
     База данных
          ↓
       Схема
          ↓
       Таблица (датасет)
          ↓
    Row Level Security
```

### Доступ к датасету

1. Откройте роль
2. Добавьте: `datasource access on [db].[schema].[table]`

### Доступ ко всей базе

1. Добавьте: `database access on [database_name]`
2. Даёт доступ ко всем таблицам

---

## Row Level Security (RLS)

### Что это

Ограничение строк данных по условию:

```
Пользователь "analyst_msk" → видит только WHERE region = 'Москва'
Пользователь "analyst_spb" → видит только WHERE region = 'СПб'
```

### Создание правила

1. **Settings → Row Level Security**
2. **"+ Row Level Security Filter"**
3. Настройте:
   - **Tables**: К каким таблицам применяется
   - **Roles**: Для каких ролей
   - **Clause**: SQL-условие (WHERE clause)

### Пример

```
Tables: orders
Roles: [Sales Managers]
Clause: region = 'Москва'
```

### С Jinja

```sql
region = '{{ current_user().department }}'
```

---

## Типичные сценарии

### Аналитик отдела

```
Создать роль: "Аналитик продаж"
Разрешения:
  - can read on Dashboard
  - can read on Slice
  - can read on SqlLab
  - datasource access on [sales].[orders]
  - datasource access on [sales].[customers]

RLS:
  - Clause: department = 'sales'
```

### Руководитель

```
Создать роль: "Руководитель"
Разрешения:
  - Скопировать с Gamma
  - Добавить доступ ко всем дашбордам подразделения

RLS: нет (видит всё)
```

### Внешний подрядчик

```
Создать роль: "Подрядчик"
Разрешения:
  - can read on Dashboard (только конкретные)
  - Минимальные datarource

RLS:
  - Жёсткие ограничения по проекту
```

---

## Управление через роли

### Группировка по функции

```
├── Роль "Все дашборды продаж"
│   └── Доступ к 5 дашбордам продаж
│
├── Роль "SQL Lab"
│   └── Доступ к SQL редактору
│
└── Пользователь
    └── Роли: "Все дашборды продаж" + "SQL Lab"
```

### Группировка по данным

```
├── Роль "Данные_Москва"
│   └── RLS: region = 'Москва'
│
├── Роль "Данные_СПб"
│   └── RLS: region = 'СПб'
│
└── Пользователь
    └── Роль: "Данные_Москва" (видит только Москву)
```

---

## Отладка прав

### Проверка доступа

1. Войдите как пользователь (или создайте тестового)
2. Проверьте, что видно/не видно

### Логирование

Включите подробное логирование:

```python
# superset_config.py
LOG_LEVEL = 'DEBUG'
```

### Типичные проблемы

| Симптом | Причина |
|---------|---------|
| Дашборд не виден | Нет `can read on Dashboard` или не опубликован |
| Пустой датасет | Нет `datasource access` или RLS фильтрует всё |
| Нет SQL Lab | Нет роли `sql_lab` |

---

## Советы

### ✅ Рекомендации

- Создавайте роли по функциям, не по людям
- Используйте RLS вместо множества датасетов
- Документируйте назначение ролей
- Начинайте с минимальных прав

### ❌ Избегайте

- Раздачи Admin всем
- Одной роли на каждого пользователя
- Прямого редактирования встроенных ролей

---

## См. также

- [[Пользователи]] — управление учётными записями
- [[Настройка конфигурации]] — параметры безопасности
- [[FAQ]] — частые вопросы
