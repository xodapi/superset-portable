---
title: Установка и настройка
status: public
created: 2026-01-28
tags: [установка, настройка, требования]
---

# Установка и настройка 🔧

Полное руководство по установке портативного Apache Superset.

---

## Системные требования

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| **Windows** | 10 (64-bit) | 11 |
| **RAM** | 2 GB | 4 GB |
| **Диск** | 500 MB | 1 GB |
| **Интернет** | Только для установки | — |
| **Права админа** | Не требуются | — |

---

## Первоначальная установка

### Шаг 1: Скачивание Python

Откройте PowerShell и выполните:

```powershell
.\setup\download_python.ps1
```

Скрипт загрузит Python 3.11 Embeddable (~25 MB).

### Шаг 2: Установка Superset

```cmd
.\setup\install_superset.bat
```

Процесс занимает 5-15 минут в зависимости от скорости интернета.

> [!IMPORTANT]
> Требуется интернет-соединение только на этом этапе!

### Шаг 3: Проверка

```cmd
start_superset.bat
```

Если браузер открылся на http://localhost:8088 — всё готово!

---

## Структура папок

```
superset-portable/
├── start_superset.bat      # 🚀 Главный запуск
├── superset-launcher.exe   # Rust-лаунчер
├── python/                 # Python + все зависимости
│   ├── python.exe
│   └── Lib/site-packages/
├── superset_home/          # Данные и настройки
│   ├── superset.db         # База метаданных
│   └── superset_config.py  # Конфигурация
├── knowledge/              # База знаний (вы здесь!)
├── docs/                   # Документация
└── setup/                  # Скрипты установки
```

---

## Конфигурация

Основные настройки в `superset_home/superset_config.py`:

```python
# Секретный ключ (генерируется автоматически)
SECRET_KEY = 'ваш-уникальный-ключ'

# Лимит строк в запросах
ROW_LIMIT = 50000

# Таймаут запросов (секунды)
SUPERSET_WEBSERVER_TIMEOUT = 60

# Включить загрузку CSV
CSV_EXTENSIONS = {'csv', 'tsv', 'txt'}
```

Подробнее: [[Настройка конфигурации]]

---

## Порты

| Сервис | Порт | Назначение |
|--------|------|------------|
| Superset | 8088 | Основной интерфейс |
| Документация | 8089 | Статическая документация |
| LightDocs | 8090 | База знаний |

---

## Обновление

Для обновления Superset:

```cmd
.\setup\update_superset.bat
```

> [!WARNING]
> Перед обновлением создайте резервную копию `superset_home/superset.db`!

---

## Перенос на другой компьютер

1. Скопируйте всю папку `superset-portable/` на USB-накопитель
2. На новом компьютере запустите `start_superset.bat`
3. Никакой установки не требуется! 🎉

---

## Решение проблем

| Проблема | Решение |
|----------|---------|
| Порт 8088 занят | См. [[Ошибки запуска]] |
| Кириллица в пути | См. [[Кириллица в путях]] |
| Не запускается | См. [[Решение проблем]] |

---

## См. также

- [[Быстрый старт]] — начало работы за 5 минут
- [[Подключение базы данных]] — как добавить свои данные
- [[FAQ]] — частые вопросы
